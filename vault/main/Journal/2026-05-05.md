---
date: 2026-05-05
type: journal
agent: main
---

---

## Routine Report — journal-audit (00:01)
**Status:** completed
**Duration:** 2m 22s


---

## Session Snapshot — 2026-05-05 00:26

_Agent: mexc-bot | Session: 2026-04-29-18-59-main-1 | Turns: 9_

## Goal
Investigar por que o sinal ARB não gerou entrada no MEXCBot, e corrigir a causa raiz na cadeia TradingView → AutoTrader → MEXCBot.

## Progress
- Diagnosticado `ClientDisconnect` no MEXCBot como sintoma (não causa)
- Identificado log real do AutoTrader em `/private/tmp/autotrader-server.log`
- Confirmado: AutoTrader decidiu GO às 20:00:18, fan-out iniciado às 20:00:20, falhou às 20:00:25 (5s = timeout exato)
- Padrão recorrente: CVNA, CRV, BABA, LAB também tiveram `MEXCBot fan-out failed`
- Entrada ARB executada manualmente via webhook direto (Trade #70, fill $0.11585)

## Decisions
- Causa raiz: `httpx.AsyncClient(timeout=5.0)` flat no `_forward_to_mexcbot` — MEXCBot sobrecarregado com polling de 7 trades não respondia em 5s
- Fix AutoTrader: timeout separado `connect=3s / write=5s / read=30s` + 2 retries com backoff 2s/4s
- Fix MEXCBot: try/except no `await request.json()` para capturar `ClientDisconnect` e logar explicitamente

## Files
- `AutoTrader/autotrader/main.py` — `_forward_to_mexcbot` reescrito com timeout granular + retry
- `MEXCBot/mexcbot/main.py` — webhook handler com try/except no body read (v0.5.4)

## Next Steps
- Monitorar próximos sinais GO no log do AutoTrader: confirmar `HTTP/1.1 200 OK` sem retry
- Verificar se Trade #70 ARB fecha via SL ou TP1 corretamente (validação do fix de plan SL da sessão anterior)
- Considerar aumentar polling interval do MEXCBot quando há muitos trades abertos (>5) para reduzir pressão no event loop

---

## Routine Report — vault-rebuild (04:01)
**Status:** completed
**Duration:** 0m 19s


---

## Session Snapshot — 2026-05-05 05:36

_Agent: crypto-bro | Session: t38 | Turns: 5_

## Goal
Resolver o bug em que o botão "Publicar" do `crypto-ta-analise-v2` enviava o link da análise do dia 03/05 quando a análise do dia 04/05 deveria ser publicada, e finalmente publicar a análise do dia 04.

## Progress
- Causa raiz identificada: `data_dir` é fixo por nome de pipeline (`workspace/data/{pipeline}/`), então cada run sobrescreve o anterior; `published.md` velho disparou idempotência incorreta.
- Implementado snapshot por-run em `compose_preview_v2.py`: copia inputs para `data/runs/{run_id}/` e embute esse path no payload do botão ACTIONS.
- Revertido patch tático de `target_date` em `publish_notion_ta_v2.py` (snapshot torna-o redundante).
- Análise do dia 04 publicada manualmente no Notion após remover `published.md` stale.

## Decisions
- Fix arquitetural feito no script da pipeline (não no harness) para minimizar blast radius — não afeta resume, outras pipelines, nem testes.
- Edit pipeline funciona automaticamente: `source_data_dir` propaga o snapshot path inalterado.
- Capa não regerada agora (SSD externo desmontado); página publicada sem cover.

## Files
- `vault/crypto-bro/Routines/crypto-ta-analise/scripts/compose_preview_v2.py` (snapshot por-run)
- `vault/crypto-bro/Routines/crypto-ta-analise/scripts/publish_notion_ta_v2.py` (revertido p/ original)
- `vault/crypto-bro/Journal/2026-05-05.md` (frontmatter + nota sobre publicação errada)

## Next Steps
- Quando SSD `/Volumes/SSD_VR4/` for montado, regerar capa BG-UP e PATCH no Notion pra adicionar à página `An-lise-04-Maio-3572da8d813481b0af83ca58c5b89e9d`.
- Validar fix em produção na próxima execução agendada de `crypto-ta-analise-v2` (verificar se snapshot subdir é criado e se botão aponta para ele).
- Considerar generalizar snapshot por-run como utilitário compartilhado se outras pipelines com botão ACTIONS surgirem.


---

## Session Snapshot — 2026-05-05 05:46

_Agent: mexc-bot | Session: 2026-04-29-18-59-main-1 | Turns: 10_

## Goal
Adicionar colunas de calor da banca (início e fim de semana) na tabela PnL por Semana da tela Equity.

## Progress
- Endpoint `/api/weekly-pnl` atualizado para consultar `AccountSnapshot` por semana
- Dois novos campos no payload: `balance_start` e `balance_end`
- Template `equity.html` atualizado com colunas "Banca Ini" / "Banca Fim"
- Bot reiniciado, dashboard HTTP 200, API retornando campos corretamente
- Commitado como v0.5.5

## Decisions
- `balance_start` = primeiro snapshot da semana (segunda-feira BRT)
- `balance_end` = último snapshot da semana (domingo BRT, ou mais recente para semana aberta)
- Janela UTC corrigida com offset +3h (BRT = UTC-3) para alinhar com lógica existente do bot
- Semanas sem snapshot exibem `—` (sem fallback para evitar dado enganoso)

## Files
- `MEXCBot/mexcbot/web/api.py` — query `AccountSnapshot` + campos `balance_start`/`balance_end` em `weekly_pnl()`
- `MEXCBot/mexcbot/web/templates/equity.html` — duas colunas novas no `<thead>` e `<tbody>`
- `MEXCBot/pyproject.toml` — versão 0.5.4 → 0.5.5

## Next Steps
- Nenhum pendente. Feature completa e validada em produção.

---

## Routine Report — vault-health (06:07)
**Status:** completed
**Duration:** 0m 21s

---

## Pipeline Report — oss-radar-v2 (07:02)
**Status:** completed (3/3)
**Duration:** 2m 04s
**Steps:** ✅ GitHub activity collection (OpenClaw + Hermes Agent) → ✅ Relevance analysis and report → ✅ Send report to Telegram (cascade-skip on NO_REPLY)
**Step timings:**
- GitHub activity collection (OpenClaw + Hermes Agent): 0m14s
- Relevance analysis and report: 1m38s
- Send report to Telegram (cascade-skip on NO_REPLY): 0m07s
**Outputs:** `workspace/data/oss-radar-v2/`


---

## Session Snapshot — 2026-05-05 07:46

_Agent: parmeirense | Session: t17 | Turns: 4_

## Goal
Criar `palmeiras-pos-jogo-v2` — pipeline que detecta fim de partida do Palmeiras e publica artigo jornalístico de pós-jogo no Notion. Requisito adicional: `palmeiras-feed-v2` proibida de escrever sobre resultados de jogos.

## Progress
- Pipeline definida com 9 steps em `parmeirense/Routines/palmeiras-pos-jogo-v2.md`
- `check_match.py` escrito com detecção dual (site oficial + SofaScore)
- `collect_reactions.py` scraping Threads via PinchTab (feed custom + @viniciusramos)
- `collect_articles.py` coletando GloboEsporte, Verdazzo, NossoPalestra
- `collect_sofascore.py` buscando incidents, statistics, lineups
- `steps/writer.md` com instrução zero-fabricação e schema de output
- `publish_notion_posjogo.py` adaptado com state/lock separados
- `scout_v2.py` atualizado com REJECT_PATTERNS para bloquear resultados de jogos
- `pos-jogo-state.json` inicializado

## Decisions
- Detecção de fim de jogo: site oficial como gate primário (`habilitaBotao=="N"` + scores), SofaScore só para `match_id`
- Estimativa de término = kickoff + 110 min
- Dedup por `match_id` E por `match_date + opponent_name` (fallback defensivo)
- State e lock separados do feed (`pos-jogo-state.json`, `/tmp/palmeiras-pos-jogo-publish.lock`)
- Schedule atual: brute-force 30 min / 15:00–01:00 — funcional mas não ideal

## Files
- `parmeirense/Routines/palmeiras-pos-jogo-v2.md` — definição da pipeline
- `parmeirense/Routines/palmeiras-pos-jogo-v2/scripts/check_match.py`
- `parmeirense/Routines/palmeiras-pos-jogo-v2/scripts/collect_reactions.py`
- `parmeirense/Routines/palmeiras-pos-jogo-v2/scripts/collect_articles.py`
- `parmeirense/Routines/palmeiras-pos-jogo-v2/scripts/collect_sofascore.py`
- `parmeirense/Routines/palmeiras-pos-jogo-v2/scripts/publish_notion_posjogo.py`
- `parmeirense/Routines/palmeiras-pos-jogo-v2/steps/writer.md`
- `parmeirense/Routines/palmeiras-feed/scripts/scout_v2.py` — REJECT_PATTERNS adicionados
- `parmeirense/pos-jogo-state.json` — state inicial

## Next Steps
- Implementar `pos-jogo-scheduler`: rotina diária (06:00) que lê calendário oficial e cria triggers precisos só em dias de jogo (kickoff + ~2h10, 2h40, 3h10)
- Verificar se o harness suporta CronCreate/RemoteTrigger a partir de script de rotina
- Testar `check_match.py` com a partida real: Sporting Cristal × Palmeiras (Libertadores, 05/05/2026)

---

## Routine Report — update-check (10:03)
**Status:** completed
**Duration:** 2m 54s


---

## Session Snapshot — 2026-05-05 10:24

_Agent: mexc-bot | Session: 2026-04-29-18-59-main-1 | Turns: 20_

## Goal
Melhorias no MEXCBot: fix IP whitelist, live entry/SL no dashboard, BE consistente e edit in-place de SL.

## Progress
- IP 138.204.73.5 identificado no erro — usuário adicionou na whitelist MEXC
- `LABUSDT` sem `.P` agora resolve via fallback no `resolve_symbol`
- Entry e SL na tabela de trades ativos agora atualizam com valores live da MEXC (full fetch)
- `real_entry_fill_price` persiste automaticamente no DB quando MEXC diverge
- BE % inconsistente corrigido: `price_safe_round` usa `math.ceil/floor` com tick size real
- `edit_plan_sl` implementado: edita plan SL in-place sem cancel gap
- 4 trades SAFE (#70, #62, #63, #66) corrigidos para BE consistente (~0.102%)

## Decisions
- `resolve_symbol("LABUSDT")` faz fallback para `LABUSDT.P` — evita re-mapear todos os tickers
- `price_safe_round` usa `math.ceil` manual (CCXT desta versão não suporta `rounding_mode=`)
- Edit in-place tem exceção: pós-TP1 (qty muda) ainda faz cancel + re-place
- `move_sl_to_be` e `on_tp1_fill` tentam edit primeiro; fallback apenas se falhar

## Files
- `mexcbot/services/mexc_client.py` — `price_safe_round`, `edit_plan_sl`, `_price_tick`, `_ccxt_symbol_to_mexc`
- `mexcbot/web/api.py` — `move_sl_to_be` (edit-first), `active_positions` (live entry/SL, DB persist)
- `mexcbot/services/real_trader.py` — `on_tp1_fill` (edit-first BE), `resolve_symbol` fallback
- `mexcbot/web/templates/trades.html` — `data-cell="entry"` e `data-cell="sl"` + update loop

## Next Steps
- Commit das mudanças desta sessão
- Monitorar próximo TP1 para confirmar edit in-place sem notificação watchdog
- Verificar se LABUSDT aparece corretamente nas próximas tentativas de entrada


---

## Session Snapshot — 2026-05-05 14:33

_Agent: mexc-bot | Session: 2026-04-29-18-59-main-1 | Turns: 40_

## Goal
Melhorias de UX e correções no MEXCBot: modal de detalhe de trade com chart ao vivo, correções de saldo, radio buttons da modal parcial, frequência de atualização dos KPIs e fix de status TP1.

## Progress
- Radio buttons da modal Parcial: ícone outlined/filled via `::before` + azul para selecionado + botão Confirmar azul + padding interno + cards full-width
- Saldo corrigido: live fetch (`equity - unrealized`) com cache 60s em `_live_balance()`, substituindo snapshot diário defasado
- KPIs (Saldo, Disponível, Positions, 7D P&L) agora têm `setInterval` próprio de 3s, independente das trades
- Modal VIEW: abre trade detail em modal com chart Lightweight Charts (lazy CDN), linhas Entry/SL/TP1/TP2, seletor 5m/15m/1h/4h, Details + Order Events em timeline
- Fix status TP1/PARTIAL com prioridade sobre `OPEN • SAFE` na tabela Active Trades
- BE button movido para após o botão CLOSE
- Order Events na modal usa layout `<ul class="timeline">` igual à tela Trades

## Decisions
- Live balance usa `equity - unrealized` (= Wallet Balance MEXC), não `cashBalance` que é Available Margin
- Chart carregado lazy (CDN unpkg) apenas no primeiro click de VIEW
- `setInterval(loadDashboardStats, 3000)` independente — desacoplado de `loadActivePositions` para sobreviver a falhas de fetch

## Files
- `mexcbot/web/api.py`: `_live_balance()`, `dashboard_stats` live, `/api/trades/{id}/detail`, `/api/candles`
- `mexcbot/web/templates/dashboard.html`: modal tradeDetail, JS `openTradeModal/closeTradeModal/_loadTdChart`, KPI interval, fix status badge, BE position, OE timeline
- `mexcbot/web/static/style.css`: `#tradeDetailModal`, `.td-iv-btn`, `.td-level`, `.td-level.*`, `#tdDetailsDl`, partial radio fixes

## Next Steps
- Commit de todas as alterações desta sessão (sem `.db/.log/.env`)
- Testar modal VIEW em trade com mexc_symbol preenchido e candles retornando dados reais
- Avaliar se intervalo padrão do chart deve ser 15m em vez de 5m (mais contexto para swing trades)
