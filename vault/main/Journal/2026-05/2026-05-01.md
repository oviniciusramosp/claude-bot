---
title: "Journal 2026-05-01"
description: Bot v3.53 vault graph fixes, agent-temp branch, watchdog startup notification restricted to primary chats, mexcbot dashboard mobile polish, OSS radar pipeline, skill audit.
date: 2026-05-01
type: journal
updated: 2026-05-01
tags: [journal, main]
agent: main
---

---

## Routine Report — journal-audit (00:02)
**Status:** completed
**Duration:** 3m 16s


---

## Session Snapshot — 2026-05-01 01:12

_Agent: contador | Session: 2026-04-14-18-54-main-1 | Turns: 1_

## Goal
Corrigir rotina `aviso-contas` que enviava mensagem "ok" no Telegram todo dia mesmo sem contas a pagar.

## Progress
- Identificado: Step 4 da rotina instruía Claude a responder `ok` incondicionalmente
- Fix aplicado: Step 4 removido; Step 3 encerra com "silenciosamente sem resposta"

## Decisions
- Problema era no prompt da rotina, não no código Python
- Solução mínima: remover apenas o step problemático, sem alterar Steps 1–3

## Files
- `vault/contador/Routines/aviso-contas.md` — removido Step 4 ("Confirmar encerramento")

## Next Steps
- Monitorar execução das 08:00 para confirmar ausência do "ok" no Telegram


---

## Session Snapshot — 2026-05-01 01:43

_Agent: mexc-bot | Session: 2026-04-29-18-59-main-1 | Turns: 50_

## Goal
Investigar e corrigir o PnL verifier do MEXCBot + melhorias de UX no dashboard web.

## Progress
- Corrigido bug crítico: `get_position_history` usava `openTime` (inexistente) em vez de `createTime` — todos os trades ficavam com `pnl_verified_at=NULL` para sempre
- Adicionado safeguard de 6h (`_PNL_VERIFY_GIVEUP_HOURS`) para evitar retry infinito
- Backfill completo: 15/15 trades verificados, drift total `$0.0044`
- Trade #38 corrigido: `-$1.14` placeholder → `+$0.0038` real (MEXC)
- Calendário + modal do dia agora incluem trades OPEN/PENDING na contagem
- 7D PnL corrigido em `api.py` e `routes.py` (duplicata) para incluir TP1/PARTIAL via `_trade_realized_pnl`; valor `$1.13` → `$4.59`
- Mark price com fallback para endpoint público MEXC (sem auth) — resolve bloqueio por IP whitelist
- Pull-to-refresh mobile adicionado em `base.html`

## Decisions
- `_CALENDAR_STATUSES` inclui OPEN/PENDING mas PnL só acumula fechados — trades abertos aparecem no count sem distorcer o heatmap
- Mark price: primário CCXT autenticado, fallback `contract.mexc.com/api/v1/contract/ticker` sem API key
- Alerta UNPROTECTED do trade #46 (PI SHORT) foi falso positivo: SL executou normalmente na MEXC enquanto o bot estava cego pelo IP whitelist

## Files
- `mexcbot/services/mexc_client.py` — fix `createTime`, fallback público mark price
- `mexcbot/services/real_trader.py` — fix matching `_row_open_ms`, safeguard 6h, Telegram alerts
- `mexcbot/web/api.py` — `_CALENDAR_STATUSES`, `_compute_stats` com `_trade_realized_pnl`, 7D query por `opened_at`
- `mexcbot/web/routes.py` — `_week_stats` sincronizado com `api.py`
- `mexcbot/web/templates/base.html` — pull-to-refresh mobile
- `mexcbot/web/templates/dashboard.html` — modal inclui OPEN, PnL floor-truncation

## Next Steps
- Adicionar `138.204.73.124` no whitelist da MEXC (prioridade alta — bot ficou cego 6min durante trade #46)
- Confirmar notificações Telegram em próxima trade real (open/TP1/close)
- Investigar `detect_manual_close` usando `sl_price` como fill placeholder para evitar recorrência do padrão do trade #38


---

## Session Snapshot — 2026-05-01 03:43

_Agent: crypto-bro | Session: t38 | Turns: 33_

## Goal
Tornar a pipeline `crypto-ta-translate` robusta a argumentos antigos e impedir vazamento do corpo traduzido no Telegram.

## Progress
- Diagnóstico: `notify: final` duplicava output e vazava conteúdo no Telegram.
- Diagnóstico: pipeline rodou com URL/conteúdo desatualizado (args stale).
- Implementadas 4 salvaguardas: remoção de `notify: final`, freshness guard de 5min, consume-on-read do args.md, banner de procedência (fetch + publish).

## Decisions
- `args.md` é consumido (deletado) após leitura — próximo run exige args novos.
- Telegram inicial no fetch mostra título PT + last_edited + URL para cancelamento precoce.
- Telegram final no publish inclui banner de procedência (PT title, last_edited, source URL) ao lado do link EN.
- Publish é o único responsável por notificar Telegram (sem `notify: final`).

## Files
- `vault/crypto-bro/Routines/crypto-ta-translate.md` — removido `notify: final`.
- `vault/crypto-bro/Routines/crypto-ta-translate/steps/fetch.md` — freshness guard, consume-on-read, provenance log + Telegram start.
- `vault/crypto-bro/Routines/crypto-ta-translate/steps/publish.md` — banner de procedência no Telegram final.
- `vault/crypto-bro/.workspace/data/crypto-ta-translate/args.md` — escrito 2x e re-disparada pipeline.

## Next Steps
- Validar as salvaguardas no próximo `traduza <URL>` real.
- Considerar replicar o mesmo padrão (freshness + consume + provenance) na pipeline `crypto-news-produce`.

---

## Routine Report — vault-rebuild (04:01)
**Status:** completed
**Duration:** 0m 20s

---

## Routine Report — vault-health (06:08)
**Status:** completed
**Duration:** 0m 28s


---

## Session Snapshot — 2026-05-01 06:13

_Agent: main | Session: 07apr-0431 | Turns: 17_

## Goal
Corrigir órfãos no graph do vault, isolar agentes, e tornar arquivos temporários visíveis como nós intencionais.

## Progress
- v3.52.1: auto-extractor escreve `[[<agent>/Notes/agent-notes|Notes]]`.
- v3.52.2: 118 arquivos backfilled com parent links path-qualified.
- v3.53.0: branch `agent-temp` criada (6 agentes) + auto-migração de 16 notas mal classificadas.
- Harness injeta `[[<agent>/agent-temp|Temp]]` após cada step/loop write.
- Bot reiniciado (PID 87780) e rodando v3.53.0.

## Decisions
- Wikilinks path-qualified resolvem colisão de basename entre agentes.
- Opção A: auto-migrar notas + revisão posterior pelo agente.
- Linkar `.workspace/data/**` via `agent-temp` em vez de esconder no Obsidian.
- `agent-<id>.md` é a única regra de detecção de agente.

## Files
- `claude-fallback-bot.py` — fix extractor, helper `_inject_temp_parent_link`, BOT_VERSION.
- `ClaudeBotManager/Sources/App/Info.plist` — bumps 3.52.1 → 3.53.0.
- `vault/CLAUDE.md` — spec de parent links + Temp branch.
- `vault/<agent>/agent-temp.md` ×6 (novo); `agent-<id>.md` ×6 (link Temp).
- 16 notas migradas main/Notes → crypto-bro/Notes & parmeirense/Notes.

## Next Steps
- Hook para `.context.md` (escrito por Claude via Bash heredoc, fora do harness Python).
- Validar visualmente no Obsidian que cores/grupos do mexc-bot aplicam após reload.

---

## Routine Report — skill-audit (06:32)
**Status:** completed
**Duration:** 2m 08s

---

## Pipeline Report — oss-radar (07:03)
**Status:** completed (2/2)
**Duration:** 3m 49s
**Steps:** ✅ GitHub activity collection → ✅ Relevance analysis and report
**Step timings:**
- GitHub activity collection: 2m31s
- Relevance analysis and report: 1m15s
**Outputs:** `workspace/data/oss-radar/`

---

## 09:55 — Watchdog startup notification restricted to primary chats

- Bug: `_notify_startup` in `claude-fallback-bot.py` sent "🟢 Bot online" to all authorized chats, including groups
- Fix: added guard `if not cid.startswith("-")` to skip group chat IDs in the first pass
- Refined further: changed iteration from `self.authorized_ids` to `self.primary_chat_ids` — startup notifications now only reach explicitly primary chats
- Motivation: secondary group chats were receiving spurious bot-online pings on every watchdog restart

---

## 09:56 — Journal audit (session consolidation)

- Nightly journal-audit routine consolidated the day's session notes into 2026-05-01.md

---

## Routine Report — journal-audit (09:57)
**Status:** completed
**Duration:** 1m 23s

---

## Routine Report — update-check (10:02)
**Status:** completed
**Duration:** 1m 56s


---

## Session Snapshot — 2026-05-01 12:18

_Agent: mexc-bot | Session: 2026-04-29-18-59-main-1 | Turns: 60_

## Goal
Melhorar dashboard mobile, corrigir notificações Telegram excessivas, consertar protocolo UNPROTECTED que pausava trading indevidamente, e usar dados reais da MEXC para colunas Realized/Unrealized.

## Progress
- Freeze de 2 colunas (ID + Symbol) com offset dinâmico via JS; Total row corrigida (colspan→células individuais, sticky visível)
- Notificação "MEXCBot online" removida do startup; watchdog notifica apenas na transição down→up
- `trading_paused` resetado manualmente (trade #52 GRIFFAIN, mesmo padrão trade #46)
- UNPROTECTED com code 2009 ("já fechado") não pausa mais o trading; registra `ALREADY_CLOSED_ON_MEXC`
- Realized e Unrealized agora usam `info.realised` e `contracts` direto da MEXC (TTL 20s)
- Diagnosticado sinal CVNA perdido: forward AutoTrader→MEXCBot funcionava, mas bot estava pausado

## Decisions
- `_POSITIONS_DATA[(symbol, side)] = {"realised": float, "contracts": float}` — fonte única para ambas as colunas
- Unrealized = `(mark - entry) × mexc_contracts` (não estimativa local), reflete parciais manuais automaticamente
- code 2009 é "posição já fechada" = seguro; qualquer outro erro de market close ainda pausa
- Notificações de startup delegadas inteiramente ao watchdog; `KeepAlive` bounces ficam silenciosos

## Files
- `MEXCBot/mexcbot/web/api.py` — `_POSITIONS_DATA`, `_refresh_positions_data`, realized+unrealized da MEXC
- `MEXCBot/mexcbot/web/static/style.css` — sticky nth-child(2), border-right freeze point
- `MEXCBot/mexcbot/web/templates/dashboard.html` — tfoot células individuais, `_updateCol2StickyLeft`
- `MEXCBot/mexcbot/main.py` — startup notification removida
- `MEXCBot/scripts/watchdog.py` — recovery notification (down→up)
- `MEXCBot/mexcbot/services/real_trader.py` — code 2009 não pausa trading

## Next Steps
- Adicionar IP `138.204.73.124` à whitelist MEXC (previne falsos UNPROTECTED)
- Tunnel URL muda a cada restart — avaliar Cloudflare Tunnel fixo ou port forward para webhook estável
- `detect_manual_close` usa `sl_price` como fill placeholder (trade #38) — fonte mais confiável pendente


---

## Session Snapshot — 2026-05-01 15:46

_Agent: crypto-bro | Session: t38 | Turns: 34_

## Goal
Endurecer a pipeline `crypto-ta-translate` para nunca rodar com URL antigo nem vazar corpo do post para o Telegram.

## Progress
- Removido `notify: final` do frontmatter (corpo não vaza mais).
- Adicionado guard de frescor de 5min em `args.md`.
- Adicionado consume-on-read: `args.md` é deletado após leitura.
- Banners de procedência no Telegram (start no fetch, final no publish) com título PT, last_edited e URL.

## Decisions
- args one-shot: TTL 5min + delete-on-read previne reuso de URL stale.
- Corpo do post vai EXCLUSIVAMENTE para o Notion; Telegram recebe só link + provenance.
- Usuário SEMPRE envia URL PT-BR — pipeline nunca deve assumir outro post.

## Files
- `Routines/crypto-ta-translate.md` — removido `notify: final`.
- `Routines/crypto-ta-translate/steps/fetch.md` — freshness guard + consume + Telegram start banner.
- `Routines/crypto-ta-translate/steps/publish.md` — Telegram final com banner de procedência.
- `.context.md` — snapshot estruturado da sessão.

## Next Steps
- Testar com próximo `traduza <URL>` real.
- Avaliar replicar padrão freshness+consume+provenance em `crypto-news-produce`.


---

## Session Snapshot — 2026-05-01 17:56

_Agent: mexc-bot | Session: 2026-04-29-18-59-main-1 | Turns: 71_

## Goal
Estabilizar webhook do TradingView, melhorar dados ao vivo da MEXC no dashboard, e adicionar conceito de trade "Safe" no MEXCBot.

## Progress
- Tunnel Cloudflare: investigado, concluído que Quick Tunnel dedicado (launchd separado) é a solução correta sem domínio — infraestrutura já estava montada e funcionando
- Notificação Telegram quando URL muda: confirmado ativo via `tunnel_watcher.sh` + teste bem-sucedido
- Calendário: dias com $0.00 recebem fundo cinza sutil (`rgba(255,255,255,0.05)`)
- Card "Win Rate por Direção": adicionado à direita do All Time com barras de progresso LONG/SHORT
- Colunas da tabela Active Trades: Entry/Margin/Opened agora usam dados ao vivo da MEXC via `_POSITIONS_DATA`; Margin sem sinal +/-
- Status "Open • Safe": implementado com badge verde, exclui do contador de posições simultâneas

## Decisions
- Sem domínio próprio = Quick Tunnel dedicado (URL estável por semanas, muda só em crash do cloudflared)
- `is_safe` calculado dinamicamente via `sl_price vs entry_price` — sem coluna nova no DB, funciona retroativamente
- SL/TP1/TP2 permanecem do DB (são os preços das ordens MEXC; objeto de posição CCXT não os expõe)
- `_POSITIONS_DATA` expandido: agora armazena `entry`, `margin`, `opened_ms` além de `realised` e `contracts`
- Safe trades não contam no `max_concurrent_positions` — liberam slot para novas entradas

## Files
- `MEXCBot/mexcbot/web/api.py` — `_POSITIONS_DATA` expandido; `is_safe` no response; `live_entry`/`live_margin`/`opened_str` do MEXC
- `MEXCBot/mexcbot/web/routes.py` — `_all_time_stats` com `long_wr`/`short_wr`/`long_wins`/`short_wins`
- `MEXCBot/mexcbot/db/models.py` — property `is_safe` no Trade model
- `MEXCBot/mexcbot/services/real_trader.py` — `open_count` exclui trades `is_safe`
- `MEXCBot/mexcbot/web/templates/dashboard.html` — card WR por direção, badge OPEN • SAFE, cinza no calendário, Margin sem sinal
- `MEXCBot/mexcbot/web/static/style.css` — `.row-3`, `.wr-bar-*`, `.badge-safe`

## Next Steps
- Verificar se `positionInitialMargin` é o campo correto para Margin na API da MEXC (testar com posição real)
- Confirmar que `timestamp` da posição MEXC bate com `opened_at` do DB (pode divergir se posição foi aberta manualmente)
- Avaliar exibir contador "X/N slots" no dashboard refletindo as Safe exclusões


---

## Session Snapshot — 2026-05-01 22:26

_Agent: mexc-bot | Session: 2026-04-29-18-59-main-1 | Turns: 73_

## Goal
Corrigir badge `OPEN • SAFE` que não aparecia mesmo com SL acima do entry (trade #45 BTC LONG). Adicionar botão manual de mover SL para breakeven no dashboard.

## Progress
- Diagnóstico: DB tinha `sl_price=74480` mas MEXC tinha `stopLossPrice=76558.4` (BE movido manualmente, DB desatualizado)
- Adicionado cache `_SL_TRIGGER` (TTL 30s) que busca trigger price ao vivo da MEXC via `get_stop_loss_attached`
- `is_safe` e campo `sl` no response da API agora usam o trigger ao vivo, não o valor stale do DB
- Botão **BE** implementado entre VIEW e CLOSE (aparece só quando `is_safe=False`)
- Endpoint `POST /api/trades/{id}/move-sl-to-be` implementado: cancela SL atual, coloca PLAN order no BE, atualiza DB, invalida cache

## Decisions
- BE offset: `entry × 1.001` (LONG) / `entry × 0.999` (SHORT) — idêntico ao fluxo automático do TP1
- Botão BE some quando `is_safe=True` para não poluir UI
- Cache `_SL_TRIGGER` separado do `_POSITIONS_DATA` (TTL 30s vs 20s) — SL muda menos frequentemente
- Em caso de falha no `place_plan_sl`, retorna erro sem fechar emergencialmente (diferente do fluxo TP1 que faz emergency close)

## Files
- `mexcbot/web/api.py` — cache `_SL_TRIGGER`, `_refresh_sl_triggers()`, cálculo live de `is_safe`, endpoint `move-sl-to-be`
- `mexcbot/web/templates/dashboard.html` — botão BE condicional, handler `moveSLtoBE()`
- `mexcbot/web/static/style.css` — estilos `.be-btn` (âmbar)

## Next Steps
- Commitar com `feat: live SL trigger cache + manual BE button`
- Considerar sync periódico reverso: atualizar `trade.sl_price` no DB quando divergir do trigger MEXC (evita acúmulo de stale entries)
- Verificar se outros trades com SL movido manualmente estão com DB stale
